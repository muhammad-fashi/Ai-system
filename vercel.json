{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && next build",
  "comment": "Cron is handled by the free GitHub Actions workflow in .github/workflows/cron.yml (it pings /api/cron/process-queue every 5 min), so no Vercel cron jobs are defined here. This avoids the Vercel Hobby plan cron restrictions. On Vercel Pro you may add a \"crons\" array here with schedules like \"* * * * *\"."
}
